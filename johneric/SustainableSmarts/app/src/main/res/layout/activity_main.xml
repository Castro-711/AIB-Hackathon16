<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#00b300"
    tools:context="aib.hackathon.johneric.sustainablesmarts.MainActivity">

    <Button
        android:text="About Us"
        android:id="@+id/aboutBtn"
        android:background="#ffffff"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="32dp"
        app:layout_constraintTop_toBottomOf="@+id/quizBtn"
        app:layout_constraintRight_toRightOf="@+id/quizBtn"
        app:layout_constraintLeft_toLeftOf="@+id/quizBtn" />

    <Button
        android:text="Go To Quiz"
        android:background="#ffffff"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/quizBtn"
        android:layout_marginTop="64dp"
        app:layout_constraintTop_toBottomOf="@+id/outdoorBulb"
        android:layout_marginStart="16dp"
        app:layout_constraintLeft_toLeftOf="parent"
        android:layout_marginLeft="16dp"
        android:layout_marginEnd="16dp"
        app:layout_constraintRight_toRightOf="parent"
        android:layout_marginRight="16dp" />

    <ImageView
        android:layout_width="0dp"
        android:layout_height="202dp"
        app:srcCompat="@drawable/greenenergysolutions"
        android:id="@+id/outdoorBulb"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        android:scaleType="centerCrop"
        app:layout_constraintHorizontal_bias="0.92" />

</android.support.constraint.ConstraintLayout>
